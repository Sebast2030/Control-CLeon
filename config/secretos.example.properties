# ==========================================================
# Plantilla de secretos locales
# ==========================================================
# Copia este archivo como config/secretos.properties (git lo ignora) y pon los
# valores reales. Tambien puedes definirlos como variables de entorno.

# Contrasena del rol cleon_app de PostgreSQL. Debe ser la misma que se paso a
# db/seguridad.sql con -v app_password=...
CLEON_DB_PASSWORD=cambia-esto
